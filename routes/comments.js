const express = require('express');
const router = express.Router();

module.exports = router;

/**
 * const getComments = require('../functions/comments/getComments');
 * const addComment = require('../functions/comments/addComment');
 * const updateComment = require('../functions/comments/updateComment');
 * const deleteComment = require('../functions/comments/deleteComment');
 * const validation = require('validation');
 * const authorization = require('authorization');
 * router.get("/get-comments", getComments);
 * router.post("/add-comment", addComment);
 * router.put("/update-comment", updateComment);
 * router.delete("/delete-comment", deleteComment);
 */